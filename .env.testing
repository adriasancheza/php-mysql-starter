# Used by PHPUnit integration tests. In CI these are provided as real
# environment variables instead; this file is a convenience for local runs
# against a local MySQL instance. Contains no secrets.

APP_ENV=testing
APP_DEBUG=true

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=php_mysql_starter_test
DB_USERNAME=root
DB_PASSWORD=
